module RealisticPush

// MOTORCYCLE ISOLATION TEST
// Intentionally inert.
// SPLAT's motorcycle-specific death-animation override is disabled so it cannot
// compete with the known-good standalone rider/bike handoff.
